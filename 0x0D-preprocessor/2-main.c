#include <stdio.h>
#include "_putchar.c"
/**
 * main - prints the name of file compiled from
 * @argc: the number of command line argument
 * @argv: an array of the command line argumenets
 *
 * Return: Always 0 successful
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
