#include "main.h"
/**
 * print_number - prints an integer
 * @n: a parameter
 *
 * Return: void
 */
void print_number(int n)
{
	if (n >= 10)
	{
		if (!(n >= 100))
		{
			_putchar('0' + (n / 10);)
		}
	}
}
